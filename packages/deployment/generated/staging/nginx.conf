# staging — proxied-subpath topology
# API is NOT exposed on a public domain. Frontend nginx proxies /basePath/api/ → localhost:3030.
# No dedicated vhost is required. PM2 listens on 127.0.0.1:3030 only.
#
# If you switch to dedicated-domain topology, replace this file by re-running:
#   node deployment/scripts/generate-runtime-files.js staging [--ssl-ready]
