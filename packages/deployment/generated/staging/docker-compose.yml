version: '3.9'
services:
  api:
    container_name: billingapp-staging
    build:
      context: /home/deploy/staging/billingapp/backend
      dockerfile: Dockerfile
    ports:
      - "3030:3030"
    env_file:
      - .env
    restart: unless-stopped
