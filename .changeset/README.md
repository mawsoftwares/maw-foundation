# Changesets

This directory is managed by [Changesets](https://github.com/changesets/changesets).

Run `pnpm changeset` when you change a published `@mawsoftwares/*` package, then merge to `main`. CI opens a version PR or publishes to GitHub Packages.

See [`docs/publishing.md`](../docs/publishing.md).
